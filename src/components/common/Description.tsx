import styled from '@emotion/styled';
import tw, { css } from 'twin.macro';

const Description = styled.div(() => [
  tw`text-center font-normal text-white text-3xl`,
  css`
    width: 1110px;
  `,
]);

export default Description;
