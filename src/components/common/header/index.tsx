import HeaderMain from './header-main';
import NavBar from './nav-bar';

const Header = () => {
  return (
    <header>
      <HeaderMain />
      <NavBar />
    </header>
  );
};

export default Header;
